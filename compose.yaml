services:
  app:
    build: shortly_java/.
    ports:
      - '8080:8080'
    environment:
      - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: 'redis:latest'
    ports:
      - '6379'
