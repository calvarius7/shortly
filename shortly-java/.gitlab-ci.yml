variables:
  BACKEND_IMAGE_NAME: "$CI_REGISTRY/$CI_PROJECT_PATH/java"
  MAVEN_REPO_LOCAL: "$CI_PROJECT_DIR/.m2/repository"
  MAVEN_CLI_OPTS: "--no-transfer-progress -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -B -V"

default:
  cache:
    - key: "m2_${CI_COMMIT_REF_SLUG}"
      fallback_keys:
        - "m2_${CI_DEFAULT_BRANCH}"
      paths:
        - .m2/repository

.eco-ci-base:
  image: eclipse-temurin:25-jdk-jammy
  extends: .rules:shortly_java
  before_script:
    - apt-get update
    - apt-get install -y git jq curl
    - update-ca-certificates
    - !reference [ .start_measurement, script ]
    - cd ./shortly-java
    - mkdir -p .m2/repository
    - chmod +x ./mvnw
  after_script:
    - !reference [ .get_measurement, script ]
    - !reference [ .display_results, script ]

# ---------- BUILD ----------

java:build:
  extends: .eco-ci-base
  stage: build
  script:
    - ./mvnw $MAVEN_CLI_OPTS -DskipTests package
  artifacts:
    expire_in: 1 day
    paths:
      - target/*.jar
      - target/classes

# ---------- TEST (parallel) ----------

java:unit-tests:
  extends: .eco-ci-base
  stage: test
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS -DskipITs=true verify
  artifacts:
    when: always
    expire_in: 1 day
    reports:
      junit:
        - target/surefire-reports/*.xml
      coverage_report:
        coverage_format: jacoco
        path: target/site/jacoco/jacoco.xml
    paths:
      - target/surefire-reports/*
      - target/*.jar
      - target/shortly

java:integration-tests:
  extends: .eco-ci-base
  stage: test
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS -DfailIfNoTests=false test-compile failsafe:integration-test failsafe:verify
  artifacts:
    when: always
    expire_in: 1 day
    reports:
      junit:
        - target/failsafe-reports/TEST-*.xml
    paths:
      - target/failsafe-reports/*

java:mutation-tests:
  extends: .eco-ci-base
  stage: test
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS -Dpitest.failWhenNoMutations=false -DskipTests org.pitest:pitest-maven:mutationCoverage
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - target/pit-reports/**

# ---------- QUALITY (parallel) ----------

# i don't like checkstyle, so i disabled it for now because who's gonna stop me?
#quality-checkstyle:
#  stage: quality
#  needs: [ "build" ]
#  script:
#    - chmod +x ./mvnw
#    - ./mvnw $MAVEN_CLI_OPTS checkstyle:check
#  artifacts:
#    when: always
#    expire_in: 1 day
#    paths:
#      - target/checkstyle-result.xml
#      - target/checkstyle*.xml

java:quality-pmd:
  extends: .eco-ci-base
  stage: quality
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS pmd:check
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - target/pmd.xml
      - target/site/pmd.html

java:quality-spotbugs:
  extends: .eco-ci-base
  stage: quality
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS com.github.spotbugs:spotbugs-maven-plugin:check
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - target/spotbugsXml.xml
      - target/spotbugsHtml.html

java:quality-dependency-check:
  extends: .eco-ci-base
  stage: quality
  needs: [ "java:build" ]
  script:
    - ./mvnw $MAVEN_CLI_OPTS org.owasp:dependency-check-maven:aggregate -Dformat=ALL -DfailBuildOnCVSS=9 -DnvdApiKey=$NVD_PERSONAL
    - ./mvnw $MAVEN_CLI_OPTS cyclonedx:makeAggregateBom
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - target/dependency-check-report.*
      - target/bom.xml

java:quality-trivy:
  stage: quality
  extends: .rules:shortly_java
  image:
    name: aquasec/trivy:latest
    entrypoint: [ "" ]
  needs: [ "java:build" ]
  script:
    - trivy fs --no-progress --exit-code 1 --severity CRITICAL --format table .
    - trivy fs --no-progress --exit-code 0 --severity HIGH,CRITICAL --format sarif -o trivy.sarif .
    - trivy fs --no-progress --exit-code 0 --severity HIGH,CRITICAL --format json -o trivy.json .
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - trivy.sarif
      - trivy.json

# ---------- DEPLOY ----------

java:deploy-native-image:
  image: docker-repository.intern.neusta.de/tn-docker:latest
  extends: .rules:shortly_java_deploy
  variables:
    DOCKER_BUILDKIT: 1
  stage: deploy
  needs: [ "java:unit-tests", "java:integration-tests" ]
  before_script:
    - apk add --no-cache git jq curl coreutils
    - !reference [ .start_measurement, script ]
    - cd ./shortly-java
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - |
      docker build \
        --progress=plain \
        --pull \
        --no-cache \
        --provenance=false \
        -t "$BACKEND_IMAGE_NAME:$CI_COMMIT_TAG" \
        -t "$BACKEND_IMAGE_NAME:latest" \
        .
    - docker push "$BACKEND_IMAGE_NAME:$CI_COMMIT_TAG"
    - docker push "$BACKEND_IMAGE_NAME:latest"
  after_script:
    - !reference [ .get_measurement, script ]
    - !reference [ .display_results, script ]

java:scan-image-remote:
  stage: deploy
  extends: .rules:shortly_java_deploy
  image:
    name: aquasec/trivy:latest
    entrypoint: [ "" ]
  needs: [ "java:deploy-native-image" ]
  variables:
    IMAGE_REF: "$BACKEND_IMAGE_NAME:$CI_COMMIT_TAG"
  script:
    # FIXME this jobs starts to fast
    - export TRIVY_USERNAME="$CI_REGISTRY_USER"
    - export TRIVY_PASSWORD="$CI_REGISTRY_PASSWORD"

    # fail fast on critical vulnerabilities but ignore will_not_fix vulnerabilities e.g. CVE-2023-45853
    - trivy image --image-src docker --no-progress --ignore-unfixed --exit-code 1 --severity CRITICAL $IMAGE_REF
    - trivy image --image-src docker --no-progress --exit-code 0 --severity HIGH,CRITICAL --format sarif -o trivy-image.sarif $IMAGE_REF
    - trivy image --image-src docker --no-progress --exit-code 0 --severity HIGH,CRITICAL --format json  -o trivy-image.json  $IMAGE_REF
  artifacts:
    when: always
    expire_in: 1 day
    paths:
      - trivy-image.sarif
      - trivy-image.json
